# E49 时点快照：A 问已答 + 训练崩坏诊断与修复菜单（2026-09-05）

> 【层位 L3｜时点快照】↑ 执行协议 = [DS_OFFICIAL_DATA_PLAN.md](DS_OFFICIAL_DATA_PLAN.md)
> §3.2（E49 预注册四轮评审定稿 + 修订记录五）；数据事实源 = [tracker/E.md](tracker/E.md) E49 节
> （11 Run 行）｜↓ 产物 = 服务器 `apt_g1/outputs/e49/` + `GR00T-WholeBodyControl/outputs/
> isaac_e49a_*`｜树根：[README.md](README.md)。状态：**诊断四步执行完毕，裁决归 owner（§3c）**。

## 0. 新会话接手入口（30 秒版）

- **已答**：去 VAE 直出 token，随机策略 **能** 学会行走（2/2 seed，it_40–60 快照
  3/3 真走路，s0 29.0m/0.47 超 E45–E47 全线）；搜索不是瓶颈。**初始化对照
  （09-05 晚）**：未训练确定性策略 3/3 零摔 14.2–14.4m/0.37 ≈ E45 终态水平
  ——it_50 的 29m = 初始的 ~2×，先验底座须计入表述。
- **未解**：训练退化机制。09-05 晚撤回"KL 病灶"诊断（§3b）并修完训练器五缺陷；
  修正基线 fix-s0 无单调崩但判据 FAIL（final 塌站立）；**lr 1e-4 降温臂负结果
  收账（09-05 深夜）：it_50 = final 双评均 6/6 零摔纯站立（vx≈0.001、0.1m），
  比 fix-s0 更早失去行走；clip 恒 0.42–0.55 ⇒ 裁剪高企非 lr 单因子**。
  **奖励地板量化（§3c）**：站立期望 rew=1.641 与 lr1e4 实测 1.644 精确吻合
  ——"训练 rew 1.6"= 站立地板非学习信号，"奖励区分度弱"升主嫌（仍按纪律
  须先用已有轨迹验证候选奖励排序才能重标）。
- **下一步（09-05 深夜 owner 决策：诊断四步已全部执行完毕，§3c；裁决归
  owner）**：① lr 臂双评负结果收账；② 2×2×3 矩阵否决环境契约/探索扰动
  两因子 = final 站立系策略真退化（init×sample 塌站立为附带发现）；③④
  分项记录（`--diag-log`）+ 冻结训练器配方验证（fix2）合并发射完毕：
  **"final 塌站立"在 timeout 修复后复现**（trunc 自举非主因）；退化路径
  全程可视（it0 地板 1.659 → it100 行走峰 1.731/d_xy 0.824 → it340–390
  探索摔期 → it680–730 akl 3700–5100 → 尾段 1.04–1.20 动而不前进）；
  **行走峰 > 站立地板 > 尾段 = "站立回报更高"在本 run 无直接支持，
  "更新后丢行走"权重上升**——下一步干预选择（更新幅度约束 vs 奖励
  重标 vs E49-B 点火）+ E49-B 启动条件（训练器已冻结+评测已可信，条件
  实质满足）由 owner 裁定。

## 1. 背景与协议

E49 = owner 2026-09-05d 裁决的「去 VAE 直接 token RL 对照」：策略随机初始化
直出 64d token → 冻结 SONIC decoder，去掉自训 VAE。三臂设计（E45 历史基线 /
E49-A 无 φ 首轮 / E49-B obs+[sinφ,cosφ] 归因臂未点火）；动作映射
`token = mean + α·std⊙a`（a 无界；官方 g1-mode tokens 标定，α=1）；协议全文与
四轮 owner 评审修订记录见 DS_OFFICIAL_DATA_PLAN §3.2 + 修订记录（一）～（四）。

冒烟（`apt_g1/isaac/e49_smoke.py`，服务器已过）：obs 153(A)/155(B)、decoder 收到
的 token = 线性映射、反馈槽 = 原始 a、B 臂 φ obs 与钟逐位一致、初始 a std
0.0182≈e⁻⁴（协议改述"初始分布远小于官方分布"的实测锚）；stats npz =
D038 官方 g1-mode tokens（2003 帧，逐维 mean/std，mean-std≈0.121）+ E27 pca
rate 0.1212。

## 2. 执行结果（owner「发现问题直接停」新标准，s1/tanh 均中止打捞）

| run | 配置 | 结果 |
|---|---|---|
| E49-A-s0 | 无界，128×2000it，seed 0，跑满 | **best it_50：3/3 存活 29.0m/0.47/h_min 0.76**；final：0/3 后退 shuffle、h_min 0.23；训练 rew 1.78→0.62 单调崩，kl med 374 |
| E49-A-s1 | 无界，seed 1，**中止@it1720** | it_50 打捞：3/3 15.3–16.1m/0.36（早期步态复现）；中止前 vx 冲 1.67 超速+fall、kl med 654 |
| E49-A-tanh-s0 | `--token-bound tanh`，**中止@it870** | 恒不摔（fall=0 全程）但 vx 0.36→0.09 衰向站立，rew 1.83→1.16；kl med 256 |

## 3. 诊断（四曲线对照，E45 latent 为锚）

E45 latent：rew 1.6→**2.07 上升**、kl med **64**、真走路（优化健康）。
E49-A 无界：rew 单调降、kl med 374–654（= latent 的 **4–10×**）。

**病灶**：PPO 每次更新的策略位移在裸 64d token 空间直接砸在 decoder 输入上，
过大且无缓冲——rew 下降说明它在随机游走而非优化。量级差两个来源叠加：
①KL 按维求和，64d vs 16d 天然 ~4×；②**冻结 VAE 原来起 KL 冲击缓冲器作用**
（z 大位移经 VAE 映射后仍是流形近邻 token，decoder 吃得下）——这是 VAE 的
隐藏第四角色（前三：流形约束/低维输出/相位钟），把 VAE vs 直出的归因又推进一层。
配置侧：每 iter ~30 次梯度步（5 epochs × minibatch 512 @ lr 3e-4）对裸 token 过热。
【挂账已裁定（§3b-6）：本节对照曲线实为 **E46**（`isaac_e45_e39_from0`），且
"每 iter ~30 步"错误（实际 6 步）；本节"病灶"归因整体撤回，修正版见 §3b】

## 3b. 诊断修正（2026-09-05 晚，owner 评审采纳；本地五处代码核验属实）

核验基线：本地 `apt_g1/isaac/` 自 561ae6c 零改动；服务器执行根
ppo_core/train/env 三文件 blob 哈希与 561ae6c 逐位一致（ea46ea14/149faed3/
c3916039）→ 以下结论适用于 E49 历史 run。

1. **指标身份错误（最关键）**：日志 `kl` =
   KL(π_θ(phase/token 头) ‖ N(prior, I))（ppo_core.py `kl_normal_std_normal`/
   `kl_normal`），是**对固定先验的偏离度，不是 PPO 新旧策略 KL**。初始
   log_std=-4 时每维 ≈3.5 → 16d 初始 ≈**56**、64d ≈**224**（策略零更新即有，
   量级差大半来自维度×初始方差）。且该指标混杂数值方差收缩（log_std 每降 1
   ≈ +1/维）——增大探索方差也能降它，读不出"每次更新幅度"。
   → **撤回预注册判据"修复 run kl med ≤~150"**（tracker E49 结论 3 同步修订）。
   增量口径（各自减初始底噪）下 token 臂漂移确实更多——"离先验更远"成立，
   但"更新过猛/随机游走"推断不成立。
2. **"每 iter ~30 次梯度步"错误**：`num_epochs=5` 存了但 update() 无 epoch
   循环，单遍 minibatch = 128×24/512 = **6 次/iter**；"epochs 5→2"原为空操作。
   "64d 过热"论断删除。
3. **GAE 边界缺陷（实锤，修复优先级最高）**：compute_gae 收了 dones 未用，
   `cont=~truncated` 同时控自举与递推 → **摔倒步不切断**（Isaac DirectRLEnv
   边界返回复位后 obs，下一局价值/回报被接到上一局）；**超时步自举被错误
   清零**（应切断递推但保留终末自举）。足以污染训练方向；是否 E49 崩坏唯一
   原因未定——latent 臂同缺陷但摔倒频率不同，"E45–E47 表现好"不能反推训练
   器正确。
4. **aux 12d 未执行却进 PPO ratio/熵**（token/latent 模式丢弃 aux 输出但联合
   logp 参与裁剪与熵奖励；E45–E47 同样带病——修正后新旧 run 不再严格可比，
   以修正训练器为新参照系）。
5. **日志 vx = 水平速度模长 × rollout 末时刻均值**（非前后向速度）；
   "vx=1.67 前进超速"表述作废。
6. **E46 勘误**：§3 的"E45 latent 锚"曲线实取自 `isaac_e45_e39_from0.log`
   = **E46**（从零+E39 VAE，03_OUTPUTS_INDEX §3 口径）。§3 挂账"不影响本
   诊断"撤回——锚曲线身份影响机制归因，kl med 64 属于 E46。

**随之降级**："PPO 在随机游走"→"观察到训练退化，机制未定"；"VAE=KL 冲击
缓冲器（隐藏第四角色）"→**未证实假设**（需实测 token/关节目标对策略位移的
敏感度）；KL-to-init 正则降级不优先（锚的是初始策略非每次更新，初始方差
极小时会锁死学习）。

**修正后的顺序（owner 决策）**：修训练器（GAE/aux/真指标 approx_kl·clip_frac·
act_std/vx_fwd）+ 确定性测试 + it_0 快照与未训练对照（确定性初始策略 ≈ 固定
官方均值 token 回放，量化 it_50 真实学习量）→ 原 LR 基线 → 再谈降温。已知
残留：truncation 自举仍为 0（复位后 obs 无终末价值可用，rsl_rl 生态同病；
稳态下 trunc 步占比 ~2.4%），若修正基线后期仍劣化再按 rsl_rl 式 reward-
bootstrap 处理；clip 不保证更新受控，必要时 KL 早停（后置）。
【**09-05 深夜更新**：trunc 自举残留已按 owner 钉死的语义修复——**复位前
终末状态的价值**（`_reset_idx` 前抓终末 obs），非 rsl_rl 式下一局观测补偿，
归入训练器正确性修复而非"以后再调"的超参；post_update_kl（整轮更新后统一
测量）同步落码。见 §3c-④ 与 tracker E49-trainer-fix2 行。】

## 3c. 诊断轮（09-05 深夜 owner 决策 + 当晚执行回写）

**owner 决策**：暂缓奖励调参与 E49-B——继续发射长训练新信息有限；项目整体
缺的是"**RL 稳定增加了什么能力**"的证据（初始化就能走 / 早期快照更好 /
后期站立，三者不能合并成"搜索不是瓶颈"或"直出已解决问题"）。三处判断
收紧：①"PPO 完全健康"证据不足（clip_frac 0.42–0.53 = 约一半样本概率比
出窗；日志 KL 是小批次统计非整轮统一测量）；②"分布近乎冻结"不能由
std/kl_prior 不变推出（均值可以移动甚至转向）；③"奖励不区分走站"目前是
候选解释（rew=全 rollout 均值 vs fwd=末时刻均值，窗口不一致，不能据此
直接重标奖励）。

**当晚执行回写（四步全部执行完毕；裁决归 owner）**：

1. **① lr 臂双评收齐 = 负结果**：it_50 与 final 均 6/6 零摔跑满 3000 步纯
   站立（vx 0.000–0.001、disp 0.098–0.11m）；训练 fwd 全程未起飞、rew
   1.644、clip 恒 0.42–0.55、iter999 loss 突跳 3628。**降温单变量失败且
   比 fix-s0（it_50 走 21.4m）更早失行走**——慢更新+分布冻结锁死初始站立。
2. **② 契约差异盘点 + 2×2×3 短对照矩阵（tracker E49-diag-matrix）**：
   - 差异表要点：训练 20s episode vs 评测 60s；cmd~U[0,0.8] 每局重采样 vs
     固定 0.8（评测点在训练分布上边缘，P(cmd>0.75)≈6%）；初态同域不同噪
     声形状（均匀 vs 高斯，量级均小）；A 测均无扰动、同 plane、动作映射/
     obs/开关全一致；**eval seed 只控初态抖动，walk clock 相位吃未播种的
     torch 全局 RNG**（同命令两次评测不逐位可复现，后续补 --seed）；训练
     日志 fwd = 128 env 混合命令 iter 末瞬时横截面，与 eval vx（单 env
     固定命令 60s 时间均值）不可直接数值比。
   - **矩阵判读（预注册分支前两条否决）**：it_50 四格全走（eval-det
     21.4m / eval-sample 21.2m / train-det 6.6m/20s / train-sample 6.6m）
     = 行走对契约+采样双重鲁棒；final 四格全站（0.09–0.22m）→ **环境
     契约差异与探索扰动均非主因，final 站立 = 策略真退化**。剩余两候选
     （"站立回报更高"vs"更新后丢行走"）交步骤③④裁决。
   - **附带发现**：init×train×sample 塌站立（0.10m）而 init×train×det 走
     6.0m——未训练策略对采样敏感，init 类策略采样评测定性失真；训练初期
     rollout 全采样，其 fwd 低 ≠ 初始策略不会走。
   - 工具沉淀：eval `--sample` + `--contract train`（20s episode + env
     原生初态 + episode 截断统计防污染）+ `--num-rollouts`/`--cmd-sample`；
     产物 `outputs/e49/diag/` 8 JSON。
3. **③④ 分项记录 + 冻结训练器配方验证（合并发射，tracker E49-A-fix2-s0）**：
   `--diag-log` 落码（10 个 d_* 量与 rew 同窗，it0 分项和 1.614 与 rew
   逐位吻合验证窗口一致）；fix2 run（fix_s0 逐字配方 + timeout 终末自举
   + post_update_kl）跑满 1000 it + 双评。**结果**：① "final 塌站立"在
   修正训练器下**复现**（final eval 纯站立 0.19–0.47m）→ trunc 自举缺陷
   非主因；② it_50 形态不同：vx 0.68–0.70 但 disp 0.5–2.2m = 有速度无
   净位移（GAE 修复改变动力学，非复现失败）；③ **退化路径首次全程可视**：
   it0 地板 1.659（d_up 0.990）→ it100 行走峰 1.731（d_xy 0.824）→
   it340–390 探索摔期（0.55–0.85，d_up 0.20–0.37）→ it680–730 akl 冲
   3700–5100 → 尾段 1.04–1.20（stand_frac 0.30–0.36、fwd≈0 = **动而不
   前进**）。**裁决要点**：行走峰 1.731 > 站立地板 1.641 > 尾段 1.04–
   1.20——奖励实际把行走排在纯站之上且尾段连纯站都不是；"站立回报更
   高"在本 run 无直接支持，"更新后丢行走"（akl/clip 爆走 + clip 恒
   0.833 在案）权重上升。**最终裁决与下一项干预选择归 owner。**
4. **④ 训练器修复二轮 + 版本冻结**：compute_gae 加 trunc_values（done→0
   优先 / trunc→复位前终末价值自举+递推切断 / t==T-1 trunc 优先于
   last_value）；`_reset_idx` 前抓终末 obs（decft 模式缺终末 history 帧
   的限制已注释，token/latent 不受影响）；e49_gae_test 10/10 PASS；
   服务器部署 sha256 双端一致。**训练器自此冻结**，旧 run（fix-s0/lr1e4）
   带 trunc 自举=0 偏差不与新 run 严格可比；fix2 = 新参照系首个 run。

**诊断分支判读表（owner 预定，发射后按行对号）**：

| 观察到 | 优先动作 |
|---|---|
| 确定性走、采样不走 | 查探索噪声经 token 映射后的动作扰动，再定探索尺度/时间相关性 |
| 同策略两环境表现不同 | 统一环境契约 |
| 同条件下站立的实际回报确实更高 | 才改奖励；先用已有轨迹验证候选奖励能正确排序；不同时加权重+收窄 σ² |
| 更新后迅速丢失行走 | 先约束更新幅度，再谈奖励 |

**资源安排（owner）**：冻结 SONIC 主线继续；D042 只推进已有质量验证，
暂不扩大数据转换；地形泛化等平地净学习增益可复现后再上。本轮验收目标 =
查清"为什么从走变站"并据此选下一项干预，**不是**再得一条难解释的
reward 曲线。

## 4. 修复菜单（09-05 早版；**§3b 评审后排序作废**：①降温降为第三步，
先修训练器正确性；`--ppo-epochs` 旗标照补但默认=1=历史单遍行为）

1. **PPO 降温（首选最小实验）**：lr 3e-4→1e-4、epochs 5→2。**发射前一步**：
   `train_apt_isaac.py` 需补 `--ppo-epochs` 旗标（PPOTrainer 默认 num_epochs=5
   未暴露 CLI；`--lr` 已有）+ eval/部署照常（sync pull → cp 三件）。
   发射命令（补旗标后）：
   ```bash
   bash /tmp/run_apt_isaac.sh .../train_apt_isaac.py --num-envs 128 --iters 1000 \
     --env apt --token-mode --token-stats .../e49/token_stats_e49.npz \
     --lr 1e-4 --ppo-epochs 2 --seed 0 --out .../isaac_e49a_cool_s0
   ```
   先 1 seed × 500–1000 it 看曲线形态（rew 上升/kl 回落即续跑，否则停）。
2. **KL-to-init 正则**：token 空间锚定初值（软 tanh），机制 = 手工重建 VAE
   缓冲作用；若①失败再上（需新写损失项）。
3. **best-snapshot 规程**：峰值总在 it_50–100，把「短训练+快照选优+early-stop」
   定为规程（零科学改动，纯省算力；无论①结果如何都可采纳）。
4. **长 episode 20s→60s**：治评测暴露面不治 kl 病灶，靠后。

## 5. 产物与代码状态

- 代码（修复轮 main = 5782e5d code + db0d562 docs；**09-05 深夜修复二轮已落
  码待提交**）：ppo_core.py（GAE done|trunc 双切断 / aux_executed / 真 epoch
  循环 / approx_kl·clip_frac·act_std，kl→kl_prior；**+trunc_values 终末自举
  + post_update_kl 整轮统一测量**）、train（--ppo-epochs、aux 置位、fwd/spd
  双口径、policy_it_0 快照；**+buf["trunc_value"] + last_value 修正 + hist
  pkl**）、eval（--init-policy；**+--sample 采样模式，mode 标注入 JSON**）、
  `e49_gae_test.py`（**10 用例**服务器 10/10 PASS，新增超时自举/done 优先/
  末步 trunc/None 回归四例）、apt_flat_env.py（**+_final_obs 终末 obs 抓取**）。
  服务器执行根五文件部署同步（sha256 双端一致：apt_flat_env 59aad6bb /
  ppo_core cf62cc01 / train ffd81977 / e49_gae_test 244e5635 + eval）。
  运行版一致性：E49 历史跑的旧代码 = 561ae6c blob（已核）；fix-s0/lr1e4 =
  修复一轮版（trunc 自举=0 偏差在身）。**训练器版本自此冻结。**
- 服务器产物（09-05 深夜新增）：`outputs/e49/eval_e49a_fix_lr1e4_s0_{it50,
  final}.json`（双评全站立负结果）+ eval 日志、`smoke_eval_sample_it50.json`
  （--sample 冒烟）；ckpt `isaac_e49a_fix_lr1e4_s0/`（policy_it_0..1000 +
  final + train_log.json，跑满收尾）；训练日志在
  `GR00T-WholeBodyControl/outputs/isaac_e49a_fix_lr1e4_s0_train.log`
  （注意：不在 apt_g1/outputs/ 下，与 fix_s0 日志路径不同）。
- 台账：tracker/E.md E49 节（+3 行：rew-floor / diag-sample / trainer-fix2，
  lr1e4 行 DONE 收账 + 结论 6）、EXPERIMENT_TRACKER E 行同步、
  DS_OFFICIAL_DATA_PLAN §3.2 修订记录（五）。
- 历史产物（E49-A 原始轮）不变：`~/ros2_data/apt_g1/outputs/e49/`（token_stats
  npz、train/eval 日志 JSON）、ckpt `isaac_e49a_s{0,1}/`、`isaac_e49a_tanh_s0/`、
  冒烟 `isaac_e49_smoke{,_b}/`；修复一轮产物：`isaac_e49a_fix_s0/` +
  eval_e49_init.json + eval_e49a_fix_s0_{it50,final}.json。

## 6. 平行线与挂账

- D042 五步顺序（M2 重定向线）照常并行但**只推进已有质量验证，暂不扩大
  数据转换**（owner 09-05 深夜资源裁定）；第三步 = 冻结校准参数 + 禁读评估
  动作官方 dof/quat（离线为主，见 DS_OFFICIAL_DATA_PLAN M2 判定块）。
- **E49-B 归因臂启动条件更新（owner 09-05 深夜）**：不必等 A 完全成功，但
  必须等**训练器与评测可信、配方冻结**——训练器已冻结（§5），评测可信由
  诊断四步交付；若 A 在修正配方下仍稳定退化，B 恰好检验显式相位是否有
  帮助；无限等待"稳定配方"会让归因实验永远无法开始。B 臂代码/冒烟全过
  （smoke_b、isaac_e49_smoke_b），点火条件 = 诊断收束 + owner 指令。
- **A/B 比较的 VAE 参照（owner）**：须补一条**相同修正训练器**的 VAE 参照
  run；E45–E47 保留为历史结果，不承担新的严格归因。每条路线比较自己的
  初始化 / 固定预算终态 / 按预定规则选的快照，区分训练种子与评测种子
  （eval 的 --seed 播种待补，见 §3c-②）。
- ~~E45 VAE 版本出入（§3 挂账）~~ → 已裁定（§3b-6）：对照曲线实为 E46
  （`isaac_e45_e39_from0`），后续归因表述统一改称 E46-latent 锚。
