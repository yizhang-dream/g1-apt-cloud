# apt_g1 / GR00T 复现项目依赖
#
# 本仓库无本地 venv（Windows 仅作代码检视/整理）。下方版本为服务器
# (cvgluser@10.16.52.225:~/ros2_data) 两个训练 venv 的实测版本，来源
# HANDOFF/04_SERVER_GUIDE.md §6。在服务器上用对应 venv 运行，不要在本机装。
#
# 安装顺序与配对坑见 HANDOFF/04 §5（如 mujoco-warp 3.5.0 ↔ mujoco 3.5.0，
# mjlab 1.2.0 ↔ warp-lang 1.12.0）。

# =========================================================================
# .venv_isaac —— Isaac Lab 主训练环境（Python 3.10）
# 用于 apt_g1/isaac/* 全部 E1-E27 训练/评测。
# =========================================================================
torch==2.5.1
isaaclab==2.1.0
warp-lang==1.16.0
onnx2torch==1.15.5
# 运行时依赖（间接）：onnxruntime、mujoco、numpy、gymnasium 等

# =========================================================================
# .venv_mjlab —— mjlab (MuJoCo-Warp) 对照环境（方向 C 从零基线）
# =========================================================================
# torch==2.13.0+cu130
mjlab==1.2.0
mujoco-warp==3.5.0
mujoco==3.5.0
warp-lang==1.12.0
rsl-rl-lib==5.0.1

# =========================================================================
# 通用（本地 MuJoCo 数据采集/分析脚本，旧 .venv_sim）
# =========================================================================
# mujoco            # MuJoCo 物理引擎
# onnxruntime       # SONIC 解码器推理
# numpy
# scipy
# matplotlib        # 地形/样本效率图
